name: movie
description: Movie app to keep tracking all new movies
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^6.0.1
  firebase_core: ^1.10.5
  cupertino_icons: ^1.0.2
  cloud_firestore: ^3.1.7
  google_fonts: ^4.0.3
  cached_network_image: ^3.2.3


dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  assets :
    - images/

  uses-material-design: true
